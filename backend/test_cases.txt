
To test your 2-layer hybrid search (Lexical + Semantic), here are the test cases updated with the **Probable Top 2 Results** based on the 50-book dataset provided.

The **Top 1** result usually prioritizes the Lexical (string match) or high-confidence Semantic hit, while the **Top 2** shows how the system handles similar metadata or thematic overlap.

### 1. Exact Match & Keyword Precision
**Goal:** Test if the exact string matching in your lexical layer is functioning perfectly.

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "The Secret Keeper" }` | **The Secret Keeper** | **A Deeper Cut** (Shared 'Fiction' tag) |
| `{ "search_query": "Cleo Coyle" }` | **On What Grounds** | **The Rock Star** (Same 'Mystery' category) |
| `{ "search_query": "626662215381115" }` | **The Secret Keeper** | *(No result / Low score)* |

### 2. Fuzzy Matching & Typo Tolerance
**Goal:** Test the "fuzziness" of your string matching (e.g., N-grams or Levenshtein distance).

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "Hary Poter" }` | **Harry Potter and the Philosopher's Stone** | **The Evertree** (Shared Magic theme) |
| `{ "search_query": "Contrivd" }` | **CONTRIVED** | **The Rock Star** (Mystery genre overlap) |
| `{ "search_query": "Beverly Luis" }` | **The Secret Keeper** | *(No result / Low score)* |

### 3. Semantic & Thematic Search
**Goal:** Test the Vector/Embedding layer. These queries contain NO keywords from the title.

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "mystery in a coffee shop" }` | **On What Grounds** | **CONTRIVED** (Semantic 'Mystery' match) |
| `{ "search_query": "boy finds out he is a wizard" }` | **Harry Potter... Stone** | **The Red Necklace** (Boy with magical gifts) |
| `{ "search_query": "french revolution fantasy" }` | **The Red Necklace** | **The Traitor's Game** (Fantasy/Adventure) |
| `{ "search_query": "Wall Street murder" }` | **CONTRIVED** | **The Wrong Man** (Crime/Thriller overlap) |

### 4. Cross-Field Intent
**Goal:** Test if the system correctly weights multi-field matches (e.g., matching Author + Category).

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "Jay Deb mystery" }` | **CONTRIVED** | **On What Grounds** (Genre match only) |
| `{ "search_query": "Brandon Sanderson fantasy" }` | **Alcatraz Versus the Scrivener's Bones** | **The Evertree** (Genre match only) |

### 5. Partial Snippets & Descriptions
**Goal:** Test the capability to find a book based on unique plot elements buried in the `description`.

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "three-headed dog" }` | **Harry Potter... Stone** | **The Evertree** (Animals/Magic theme) |
| `{ "search_query": "Village Blend coffeehouse" }` | **On What Grounds** | *(No result)* |
| `{ "search_query": "Paris 1789" }` | **The Red Necklace** | **The Eustace Diamonds** (Historical tag) |

### 6. Categorical & Abstract Queries
**Goal:** Test how well the system clusters books by category even with vague inputs.

| Search Query | Top 1 Result (Probable) | Top 2 Result (Probable) |
| :--- | :--- | :--- |
| `{ "search_query": "juvenile magic" }` | **Harry Potter... Stone** | **Alcatraz Versus... Bones** |
| `{ "search_query": "historical mystery" }` | **Jack the Ripper** | **The Red Necklace** |
| `{ "search_query": "erotic romance" }` | **Take Me Again** | **Three Days** |

### 7. Robustness & Edge Cases
**Goal:** Ensure the system doesn't crash or return irrelevant junk for low-signal queries.

| Search Query | Expected Behavior |
| :--- | :--- |
| `{ "search_query": "the" }` | Should return most popular titles or handle via stop-word filtering. |
| `{ "search_query": "!!!!!" }` | Should return 0 results or ignore punctuation. |
| `{ "search_query": "book about 1980" }` | Should retrieve **CONTRIVED** (published_year is 1980). |

### Summary for your Evaluation
* **If Top 1 is wrong for Lexical:** Your string matching (BM25 or similar) might need more weight.
* **If Top 1 is wrong for Semantic:** Your embedding model (or the way you're concatenating `title` + `description`) needs refinement.
* **Optimal Hybrid Performance:** For a query like "Wall Street murder", the system should ideally rank **CONTRIVED** first because "Wall Street" is a specific keyword match in the description and "murder" is a semantic match for the Mystery category.